Q29: Write a program to calculate the factorial of a number.


#include<stdio.h>
int main()
{
	int a,fact=1,i=1;
	printf("enter the number you wnt the factorial of:");
	scanf("%d",&a);
	while(i<=a)
	{  fact=fact*i;
	  i+=1;
	}
	printf("the factorial is %d",fact);
}
